# ===========================================
# 预发布环境配置模板
# ===========================================
# 预发布环境的配置，介于开发和生产环境之间

# ===========================================
# A类 - 安全敏感配置
# ===========================================
# 预发布环境应使用接近生产环境的密钥，但可以使用测试密钥

JWT_SECRET=${JWT_SECRET}
DB_PASSWORD=${DB_PASSWORD}
LLM_API_KEY=${LLM_API_KEY}
API_KEY=${API_KEY}
VITE_API_KEY=${VITE_API_KEY}

# ===========================================
# B类 - 环境特定配置
# ===========================================
NODE_ENV=staging

# 预发布环境服务端点
DATABASE_URL=${DATABASE_URL}
REDIS_URL=${REDIS_URL}
API_BASE_URL=${API_BASE_URL}
WEB_BASE_URL=${WEB_BASE_URL}

# 预发布环境日志配置
LOG_LEVEL=info
LOG_FORMAT=json

# 预发布环境服务器配置
HOST=0.0.0.0
PORT=${PORT}

# 预发布环境 CORS 配置
CORS_ORIGIN=${CORS_ORIGIN}

# ===========================================
# C类 - 共享业务配置
# ===========================================
PAGINATION_LIMIT=15
PAGINATION_MAX_LIMIT=75
REQUEST_TIMEOUT=45000
UPLOAD_TIMEOUT=300000
MAX_RETRIES=3
RETRY_DELAY=1000
CACHE_TTL=2700
CACHE_MAX_SIZE=750

# 预发布环境功能开关
FEATURE_REGISTRATION=true
FEATURE_EMAIL_VERIFICATION=true
FEATURE_2FA=false

# ===========================================
# D类 - 开发工具配置
# ===========================================
DEBUG=false
VERBOSE_LOGGING=true
MOCK_API=false
SEED_DATA=false
HOT_RELOAD=false

# ===========================================
# LLM 配置（预发布环境）
# ===========================================
LLM_DEFAULT_PROVIDER=deepseek
LLM_DEFAULT_MODEL=deepseek-chat
LLM_BASE_URL=https://api.deepseek.com/v1
LLM_TIMEOUT=45000
LLM_MAX_RETRIES=3
LLM_TEMPERATURE=0.7
LLM_MAX_TOKENS=2000
LLM_TOP_P=1.0
LLM_FREQUENCY_PENALTY=0.0
LLM_PRESENCE_PENALTY=0.0

# ===========================================
# 应用配置（预发布环境）
# ===========================================
VITE_APP_TITLE=Fastify React App (Staging)
VITE_APP_VERSION=1.0.0-staging
VITE_APP_DESCRIPTION=预发布环境 - 现代化全栈应用
JWT_EXPIRES_IN=3d

# ===========================================
# 预发布环境说明
# ===========================================
# 1. 预发布环境用于生产前的最终测试
# 2. 配置应尽可能接近生产环境
# 3. 可以使用测试密钥，但应模拟生产环境的行为
# 4. 日志级别设置为 info，便于问题排查
# 5. 功能开关可以部分启用，用于测试新功能
