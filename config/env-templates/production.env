# ===========================================
# 生产环境配置模板
# ===========================================
# 生产环境的配置模板，包含生产环境的安全设置
# 注意：敏感信息必须通过环境变量或密钥管理服务提供

# ===========================================
# A类 - 安全敏感配置（必须运行时提供）
# ===========================================
# 这些配置包含敏感信息，必须通过环境变量或密钥管理服务提供
# 生产环境强制验证，不允许使用占位符

# JWT 密钥（必需 - 必须设置强随机密钥）
JWT_SECRET=${JWT_SECRET}

# 数据库密码（必需 - 必须设置强密码）
DB_PASSWORD=${DB_PASSWORD}

# LLM API 密钥（必需 - 必须设置真实密钥）
LLM_API_KEY=${LLM_API_KEY}

# 其他 API 密钥（可选）
API_KEY=${API_KEY}
VITE_API_KEY=${VITE_API_KEY}

# ===========================================
# B类 - 环境特定配置
# ===========================================
NODE_ENV=production

# 生产环境服务端点（请根据实际部署环境修改）
DATABASE_URL=${DATABASE_URL}
REDIS_URL=${REDIS_URL}
API_BASE_URL=${API_BASE_URL}
WEB_BASE_URL=${WEB_BASE_URL}

# 生产环境日志配置
LOG_LEVEL=warn
LOG_FORMAT=json

# 生产环境服务器配置
HOST=0.0.0.0
PORT=${PORT}

# 生产环境 CORS 配置
CORS_ORIGIN=${CORS_ORIGIN}

# ===========================================
# C类 - 共享业务配置
# ===========================================
PAGINATION_LIMIT=20
PAGINATION_MAX_LIMIT=100
REQUEST_TIMEOUT=30000
UPLOAD_TIMEOUT=300000
MAX_RETRIES=3
RETRY_DELAY=1000
CACHE_TTL=3600
CACHE_MAX_SIZE=1000

# 生产环境功能开关
FEATURE_REGISTRATION=true
FEATURE_EMAIL_VERIFICATION=true
FEATURE_2FA=true

# ===========================================
# D类 - 开发工具配置（生产环境禁用）
# ===========================================
DEBUG=false
VERBOSE_LOGGING=false
MOCK_API=false
SEED_DATA=false
HOT_RELOAD=false

# ===========================================
# LLM 配置（生产环境）
# ===========================================
LLM_DEFAULT_PROVIDER=deepseek
LLM_DEFAULT_MODEL=deepseek-chat
LLM_BASE_URL=https://api.deepseek.com/v1
LLM_TIMEOUT=30000
LLM_MAX_RETRIES=3
LLM_TEMPERATURE=0.7
LLM_MAX_TOKENS=2000
LLM_TOP_P=1.0
LLM_FREQUENCY_PENALTY=0.0
LLM_PRESENCE_PENALTY=0.0

# ===========================================
# 应用配置（生产环境）
# ===========================================
VITE_APP_TITLE=Fastify React App
VITE_APP_VERSION=1.0.0
VITE_APP_DESCRIPTION=现代化全栈应用
JWT_EXPIRES_IN=7d

# ===========================================
# 生产环境部署说明
# ===========================================
# 1. 所有 ${VARIABLE_NAME} 格式的变量必须通过环境变量提供
# 2. 敏感信息（A类配置）必须使用强密码和随机密钥
# 3. 建议使用密钥管理服务（如 AWS Secrets Manager、Azure Key Vault）
# 4. 数据库连接字符串应包含 SSL 配置
# 5. 生产环境应启用所有安全功能
# 6. 日志级别建议设置为 warn 或 error
# 7. 确保所有服务端点使用 HTTPS
