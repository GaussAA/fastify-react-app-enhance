# ===========================================
# CI/CD 环境配置模板
# ===========================================
# CI/CD 环境的配置，专门用于持续集成和部署

# ===========================================
# A类 - 安全敏感配置（CI环境占位符）
# ===========================================
# CI环境使用环境变量注入，这里提供占位符

JWT_SECRET=${JWT_SECRET}
DB_PASSWORD=${DB_PASSWORD}
LLM_API_KEY=${LLM_API_KEY}
API_KEY=${API_KEY}
VITE_API_KEY=${VITE_API_KEY}

# ===========================================
# B类 - 环境特定配置
# ===========================================
NODE_ENV=ci

# CI环境服务端点
DATABASE_URL=${DATABASE_URL}
REDIS_URL=${REDIS_URL}
API_BASE_URL=${API_BASE_URL}
WEB_BASE_URL=${WEB_BASE_URL}

# CI环境日志配置
LOG_LEVEL=warn
LOG_FORMAT=json

# CI环境服务器配置
HOST=0.0.0.0
PORT=${PORT}

# CI环境 CORS 配置
CORS_ORIGIN=${CORS_ORIGIN}

# ===========================================
# C类 - 共享业务配置
# ===========================================
PAGINATION_LIMIT=10
PAGINATION_MAX_LIMIT=50
REQUEST_TIMEOUT=15000
UPLOAD_TIMEOUT=120000
MAX_RETRIES=2
RETRY_DELAY=500
CACHE_TTL=300
CACHE_MAX_SIZE=200

# CI环境功能开关
FEATURE_REGISTRATION=true
FEATURE_EMAIL_VERIFICATION=false
FEATURE_2FA=false

# ===========================================
# D类 - 开发工具配置
# ===========================================
DEBUG=false
VERBOSE_LOGGING=false
MOCK_API=false
SEED_DATA=false
HOT_RELOAD=false

# ===========================================
# LLM 配置（CI环境）
# ===========================================
LLM_DEFAULT_PROVIDER=deepseek
LLM_DEFAULT_MODEL=deepseek-chat
LLM_BASE_URL=https://api.deepseek.com/v1
LLM_TIMEOUT=15000
LLM_MAX_RETRIES=2
LLM_TEMPERATURE=0.1
LLM_MAX_TOKENS=1000
LLM_TOP_P=1.0
LLM_FREQUENCY_PENALTY=0.0
LLM_PRESENCE_PENALTY=0.0

# ===========================================
# 应用配置（CI环境）
# ===========================================
VITE_APP_TITLE=Fastify React App (CI)
VITE_APP_VERSION=1.0.0-ci
VITE_APP_DESCRIPTION=CI环境 - 现代化全栈应用
JWT_EXPIRES_IN=30m

# ===========================================
# CI环境说明
# ===========================================
# 1. CI环境专门用于持续集成和部署
# 2. 所有敏感信息通过环境变量注入
# 3. 超时时间较短，便于快速构建
# 4. 日志级别设置为 warn，减少构建输出
# 5. 功能开关保持最小化，确保构建稳定性
# 6. JWT 过期时间很短，仅用于构建测试
