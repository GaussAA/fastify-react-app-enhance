# ===========================================
# 环境变量配置模板
# ===========================================
# 复制此文件为 .env 并根据需要修改配置

# ===========================================
# 必需配置（敏感信息，必须设置）
# ===========================================
# JWT 密钥（必需 - 请修改为随机字符串）
JWT_SECRET=your_jwt_secret_key_here

# 数据库密码（必需 - 请修改为安全密码）
DB_PASSWORD=your_secure_password_here

# LLM API 密钥（必需 - 敏感信息）
LLM_API_KEY=your_llm_api_key_here

# ===========================================
# 可选配置（有默认值，可根据需要修改）
# ===========================================
# 环境配置
NODE_ENV=development
LOG_LEVEL=info
HOST=0.0.0.0
PORT=8001

# API 密钥（可选）
API_KEY=your_api_key_here
VITE_API_KEY=your_frontend_api_key_here

# LLM 配置（可选，有默认值）
LLM_DEFAULT_PROVIDER=deepseek
LLM_DEFAULT_MODEL=deepseek-chat
LLM_BASE_URL=https://api.deepseek.com/v1
LLM_TIMEOUT=30000
LLM_MAX_RETRIES=3
LLM_TEMPERATURE=0.7
LLM_MAX_TOKENS=2000
LLM_TOP_P=1.0
LLM_FREQUENCY_PENALTY=0.0
LLM_PRESENCE_PENALTY=0.0

# 应用配置（可选，有默认值）
VITE_APP_TITLE=Fastify React App
VITE_APP_VERSION=1.0.0
VITE_APP_DESCRIPTION=现代化全栈应用
JWT_EXPIRES_IN=7d

# ===========================================
# 自动生成的配置（请勿手动修改）
# ===========================================
# 以下配置由统一配置管理系统自动生成
# 数据库连接字符串（端口由统一配置管理）
DATABASE_URL=postgresql://postgres:your_secure_password_here@localhost:15432/fastify_react_app

# Redis 连接字符串（端口由统一配置管理）
REDIS_URL=redis://localhost:6379

# API 基础 URL（端口由统一配置管理）
VITE_API_BASE_URL=http://localhost:8001/api
VITE_API_HOST=localhost
VITE_API_PORT=8001

# CORS 配置（端口由统一配置管理）
CORS_ORIGIN=http://localhost:5173

# ===========================================
# 脚本配置（启动/停止脚本）
# ===========================================
# 服务端口配置
API_PORT=8001
WEB_PORT=5173
DOCS_PORT=8080
POSTGRES_PORT=15432
REDIS_PORT=6379

# 服务主机配置
API_HOST=localhost
WEB_HOST=localhost
DB_HOST=localhost
REDIS_HOST=localhost

# Docker配置
DOCKER_COMPOSE_FILE=infrastructure/docker/docker-compose.yml
POSTGRES_SERVICE=postgres
REDIS_SERVICE=redis
POSTGRES_CONTAINER=postgres
REDIS_CONTAINER=redis

# 超时配置
POSTGRES_WAIT_TIME=30
REDIS_WAIT_TIME=15

# 包管理器配置
PACKAGE_MANAGER=pnpm
DEV_COMMAND=dev

# 必需环境变量列表（用逗号分隔）
REQUIRED_ENV_VARS=JWT_SECRET,DATABASE_URL,LLM_API_KEY

# 额外要停止的端口（用逗号分隔）
EXTRA_PORTS_TO_STOP=3000,3001,4000,5000,8000,9000

# ===========================================
# 配置说明
# ===========================================
# 1. 必需配置：JWT_SECRET, DB_PASSWORD, LLM_API_KEY
# 2. 可选配置：有默认值，可根据需要修改
# 3. 端口配置由统一配置管理系统自动管理
# 4. 敏感信息请使用强密码和随机密钥
# 5. 生产环境请使用环境变量或密钥管理服务
# 6. 脚本配置：启动/停止脚本的所有配置选项
